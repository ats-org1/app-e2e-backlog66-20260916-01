# src/PP-Solutions/

Power Platform Solution のソースを置く場所です。**払い出し時点では空**で、リリース操作でソースが保存されると `src/PP-Solutions/<Solution の Unique Name>/` として追加されます。

Solution の名前は案件側で決めます。テンプレート側にひな型や固定の名前を持たせていないのは、案件ごとの Solution 名を縛らないためです。

保存とデプロイの操作方法は、基盤管理担当から案内されるリリース運用ガイドを参照してください。
